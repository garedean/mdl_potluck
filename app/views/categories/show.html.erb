<ul>
  <% @items.each do |item| %>
    <li>
      <% if item.has_children %>
        <%= link_to item.name, category_path(item) %>
      <% else %>
        <%= link_to item.name, new_category_ingredient_path(item, Ingredient.new) %>
      <% end %>
    </li>
  <% end %>
</ul>
