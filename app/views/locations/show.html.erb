<div class="breadcrumbs">
  <% @location.ancestors.each do |location| %>
    <%= link_to location.name, location %> &gt;
  <% end %>
</div>

<ul class="theList">
  <% @location.children.order(:name).each do |location| %>
    <li>
      <%= hierarchy_link(location, :location, nil, "button listButtons") %>
    </li>
  <% end %>
</ul>
