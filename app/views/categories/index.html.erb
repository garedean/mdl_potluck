<ul class="breadcrumbs">
<br>
</ul>

<div class="listContainer">
	<ul class="theList" id="smoothScroll">
		<% @food_categories.each do |category| %>
		  <li>
				<%= link_to category_path(category) do %>
		  		<span class="button listButtons">
		  		  <%= category.name %>
		  		</span><br>
		  	<% end %>
		  </li>
		<% end %>
	</ul>
</div>
