
<div class="breadcrumbs">
  <%= link_to "ITEM SUMMARY", :back %>

  <%= link_to relocate_ingredient_locations_path(@ingredient) do %>
    <div class="cartNavButton"><i class="fi-marker"></i>  MOVE</div>
  <% end %>

  <%= link_to ingredient_path(@ingredient), method: :delete, data: {confirm: "Are you sure?"} do %>
    <div class="cartNavButton"><i class="fi-trash"></i>TRASH</div>
  <% end %>
</div>

<div class="itemContainer">
  <h2>Box <%= @ingredient.id %></h2>
  <h2><%= @ingredient.name %></h2>

  <% @ingredient.location.ancestors.each do |location| %>
    <%= location.name %> &gt;
  <% end %>

  <%= @ingredient.location.name %>
  <%= link_to "Place in location.", relocate_ingredient_locations_path(@ingredient) unless @ingredient.assigned_location? %>

  <div>
    Expires on <%= @ingredient.expiration_date.strftime("%b %d, %Y") %>

    <% if @ingredient.expiration_date %>
      <% if @ingredient.expiration_date.future? %>
        <div class="freshItem">FRESH</div>
      <% else %>
        <div class="expiredItem">EXPIRED</div>
      <% end %>
    <% else %>
      <strong>N/A</strong>
    <% end %>
    <br><br>
  </div>

  <div id="quantityFade" class="">

    <h6>ALL LOCATIONS COUNT:
    <strong><%= Ingredient.count_all_locations(@ingredient) %></strong></h6>
  </div>
</div>

<% if @show_quick_add %>
  <%= render "quick_add_lightbox" %>
<% end %>
